apiVersion: v1
kind: Service
metadata:
  name: py-service-07
  namespace: ns07
spec:
  type: NodePort
  selector:
    app: python-app-07
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30107
